// 30.	Write a program to check whether a character is in the alphabet or not.
let a = "N";

if ((a >= "a" && a <= "z") || (a >= "A" && a <= "Z")) {
  console.log(" this is alphabet");
} else {
  console.log("this is not alphabet");
}
